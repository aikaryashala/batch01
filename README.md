🗂️ Topics & Categories Index

---

### 🖥️ C Programming
- Declaration, errors, warnings,  
- `struct`, `typedef`, user-defined types
- Billing program, file I/O
- ASCII chart, endianness (little/big)
- Return address manipulation
- `fopen`, `fprintf`, `fscanf`, `fclose`, `fwrite`
- Static and global variables — data segment
- Multi-file compilation, preprocessor (`.i` files)
- Debugging with `lldb`/`gdb` — breakpoints, `bt`, `memory read`
- Execution model: compilation → native binary

---

### 🐍 Python
- Classes, objects, instances, methods, functions, recursion function, consturctor, interface, inheritance
_ Encapsulation, polymorphism 
_ Exception handlers 
- List, sets, dictionary, tuple, slicing, string 
- Bytecode disassembly (`dis` module)
- PDB debugger — `n`, `s`, `p`, `locals()`, `args` 
- Error types and tracebacks
- File I/O with `sqlite3` module
- REPL usage
- Execution model: interpreted bytecode → CPython VM

---

### 🗄️ Databases & SQL
- SQLite basics: `CREATE TABLE`, `INSERT`, `SELECT`, `UPDATE`, `DELETE`
- Primary keys and constraints
- Indexing: `CREATE INDEX`, `EXPLAIN QUERY PLAN`
- Performance: query timing with vs. without index
- WhatsApp `msgstore.db` exploration
- Schema metadata (`sqlite_master`)
- Python `sqlite3` module integration

---

### 🐧 Linux & Shell
- `grep`, `find` with flags and piping
- `chmod`, `chown`, `stat` — file metadata
- `$PATH`, `.bashrc`, custom binaries
- `readelf`, `nm`, ELF binary structure
- Redirection: `>`, `<`, `2>`, `2>&1`
- `echo`, `tac`, `head`, `tail`, `more`
- `pkill`, `systemctl`, `sshd`
- `exiftool`, `xxd`, `hexedit`
- Package management: `apt`, `dpkg`, local package index
- Process and memory: `/proc/<pid>/maps`
- VIM basics

---

### 🗂️ File Systems
- Inodes, directory entries, data blocks
- Block allocation and fragmentation
- HFS+, HFSX (case sensitivity)
- NTFS encryption (EFS)
- File metadata vs. file contents
- File formats: ELF, MP4, MP3, PDF, DOCX, PNG, JPG, APK

---

### 🌐 Networking & HTTP
- HTTP request/response cycle, headers, status codes
- `netcat` for raw TCP visibility
- `curl`, HTTPie, Postman
- Port forwarding, NAT, public vs. private IPs
- Ed25519 cryptography, key generation
- Checksums: CRC32, SHA-256
- Client timeout behaviour
- Browser DevTools → Network tab
- SSH — flags, tunnels, key types

---

### 🧠 AI, LLMs & Deep Learning
- LLM, SLM, TLM, Agents, RAG
- MCP (Model Context Protocol)
- "Attention Is All You Need" — Transformer architecture
- Deep learning history
- Image segmentation with Vision APIs
- CAPTCHAs as data labelling mechanisms
- Model sizes, quantisation, local inference
- Hugging Face — finding and using open models
- Prompt engineering and prompt collections

---

### ⚙️ Computer Architecture & Low-Level
- Memory segments: stack, heap, data (`.bss`, `.data`), code (`.text`)
- Call stack and return addresses
- L1/L2/L3 cache — sizes and speeds
- Little-endian vs. big-endian
- Logic gates, 2's complement
- ART vs. JVM (Android runtime)
- ARM SoC chips
- Execution models: C, Python, Java
- Compiler bootstrapping

---

### 🌍 Web & Frontend
- HTML tags: `<p>`, `<pre>`, `<br>`
- URI, URL, links
- DOM — structure and JavaScript manipulation
- DOM events: keyboard, mouse, button, form elements
- Responsive vs. non-responsive websites
- GitHub Pages with Markdown
- `index.html` naming convention
- Browser DevTools — Network tab

---

### 🔐 Security & Cryptography
- Ed25519 — elliptic curve, key generation
- AES-256, RSA-2048 — computational hardness
- Quantum computing threats to encryption
- Buffer overflow and return address manipulation
- NTFS encryption (EFS)
- SSH key types and usage
- Checksums — CRC32, MD5, SHA-256

---

### 🛠️ Tools & Workflows
- Git/GitHub — push, pull, forks, pull requests
- Asciinema — recording terminal sessions
- Gamma — presentation creation
- `lldb` / `gdb` — debugging
- PDB — Python debugger
- Postman — API testing
- `xxd`, `hexedit` — binary inspection
- VIM — modal editing
- `sqlite3` CLI
- CMake — multi-platform C builds

---

### ☁️ Cloud & DevOps
- `systemctl` service management
- Deploying and exposing local servers
- Open-source contribution workflow

---
 once's check it and anything missing tell me
