
### October 2025

#### 13 Oct 2025

- [ ] Get the SQLite database file from your WhatsApp onto your computer and explore the file.
- [ ] Make cold calls as a recruiter to 5 companies of size 50–100 employees.

---

#### 14 Oct 2025

- [ ] How does HFSX differ from HFS+ in terms of case sensitivity?
- [ ] What happens when an inode is corrupted or lost?
- [ ] In which version of NTFS was encryption introduced?

---

#### 16 Oct 2025

- [ ] What are the different types of storage available in AWS services — with read speed, write speed, and cost?
- [ ] What are Linux commands like `chmod` that change the metadata of a file?

---

#### 19 Oct 2025

- [ ] What happens to the data in main memory when a program ends or crashes?
- [ ] Why do we use hard disks or SSDs to store data permanently?
- [ ] What is a file system in simple words?
- [ ] What is the difference between a file name and the contents of the file?
- [ ] Why are files stored in blocks instead of a byte at a time?

---

#### 21 Oct 2025

- [ ] Explore `man grep` and `man find`.
- [ ] Create your own custom binaries, put them in a folder, and add that folder to the `$PATH` variable using `.bashrc`.

---

#### 22 Oct 2025

- [ ] Write functions for `prime` and `product` in C, debug them, and run a backtrace.

---

#### 23 Oct 2025

- [ ] Explore URI, URL, and link — what are the differences?
- [ ] Record a video on the topic: "Is code and a program the same thing?"
- [ ] Comment out declarations in a C file, compile it, and write down all the errors and warnings produced.

---

#### 30 Oct 2025

- [ ] Explore `fopen`, `fprintf`, and `fclose` functions in C.
- [ ] Write a program to print a bill — take item name, number of items, and cost per item for three items as an example.
- [ ] How do you convert a file into an executable using `chmod`?
- [ ] Create a binary named `sum.exe` on Ubuntu, push to GitHub, pull to local machine, run the binary, and record your observations.

---

#### 31 Oct 2025

- [ ] Debug the following code and print the memory at every line using the `memory read` command:
  ```c
  char ch;
  scanf("%c", &ch);
  ch = ch - (0x20);
  ch = ch - ('a' - 'A');
  ```
- [ ] Explore the following file formats: MP4, MP3, PDF, DOCX, JPG, PNG.
- [ ] Take inputs `num1 = 0x44434241`, `num2 = 0x68676665`. Store these numbers in a `.txt` file using `fprintf`, `fwrite`, and `fclose`.
- [ ] Save `ABCD` in a `.txt` file and inspect the hex values using `xxd`.
- [ ] `man xxd` — read and understand the manual.
- [ ] Save the return value of `fprintf` and observe what it returns.
- [ ] What do `fprintf`, `fscanf`, `printf`, and `scanf` return?

---
