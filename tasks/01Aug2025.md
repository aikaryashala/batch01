### 🔧 Systems & Compilers

- [ ] When the C language was first created, how was the compiler built and using which languages?
- [ ] Explore `grep` and `find` commands.
- [ ] Create your own custom binaries, put them in a folder, and add that folder to the `$PATH` variable.
- [ ] How does image compression work?

---

### 💻 C Programming

- [ ] Write down declaration statements, commenting statements, errors, and warnings in C programming.
- [ ] Debug `product_function.c` — identify local variable addresses, return addresses, and verify the increasing/decreasing order of addresses.
- [ ] Write a billing program that takes item name, number of items, cost per item, and prints the total cost and total bill.
- [ ] For `sum.c`, create an `input.txt` which contains the inputs to `sum.c` using `>` and `<` redirection operators with `./sum`.
- [ ] Find a particular record using a hash table in C.
- [ ] Create a binary `sum.exe` on Ubuntu VM, push to GitHub, pull to local machine, and use `hexedit` to inspect the binary file.
- [ ] Debug the following code and print the memory of the variable at every line using `memory read <address of ch>`:
  ```c
  #include <stdio.h>
  int main() {
      char ch;
      scanf("%c", &ch);
      ch = ch - ('a' - 'A');
      printf("%c", ch);
      return 0;
  }
  ```

---

### 🗂️ File Systems

- [ ] How are files named and stored on disk?
- [ ] How does the OS locate a file on disk?
- [ ] How is disk space allocated and managed?

---

## 📅 2025 Tasks

---

### August 2025

#### 28 Aug 2025

- [ ] Daily: Create a 2–3 minute video on a topic and upload it to your YouTube channel.

