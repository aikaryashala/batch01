### March 2026

#### 2 Mar 2026

- [ ] Write down all the new concepts, protocols, libraries, and frameworks encountered — maintain a running glossary.
- [ ] Build a CLI client for your real estate backend (in addition to the existing web client).
- [ ] Who is the person who found the new approach to training neural networks that gave rise to the deep learning revolution?
- [ ] Read and understand the paper "Attention Is All You Need".
- [ ] CAPTCHAs are used to collect annotated training data — explore how this works.
- [ ] Explore image segmentation with Vision APIs.
- [ ] Research the AI models studied in your batch. For each model, document:
  - Versions (previous, current, next if announced)
  - Release date and creator organisation
  - Core speciality
  - List of companies and products using the model and what problem they are solving
- [ ] What are the maximum and minimum sizes (in GB) for base LLM models?
- [ ] What are alternatives to GitHub?
- [ ] Create a GitHub repo with a curated collection of prompts for different use cases.
- [ ] Daily: spend one hour reading open-source code.

---

#### 10 Mar 2026

- [ ] Read about `struct` in C — its applications and the contexts in which it is most commonly used.
- [ ] Experiment with classes, objects, and instances in Python.

---

#### 11 Mar 2026

- [ ] `typedef` in C — create user-defined data types and experiment with them.

---

#### 13 Mar 2026

- [ ] Write a C program where `main` has 2 local variables. Define a second function (called by `main` twice) that has a local variable and a static variable. Declare a global variable used in both functions.
  - **a)** Compile the program and verify that global and static variables are present in the data section.
  - **b)** Debug the program and print the addresses of all variables. Verify that variables are in their respective memory segments while the program is running.
  - **c)** How do you find the data segment address range?

- [ ] Find a GitHub repository of an application written in C that works on multiple platforms (Windows, macOS, Linux).

- [ ] Compile two different C programs — one that includes only `<stdio.h>`, and another that includes both `<stdio.h>` and `<math.h>`. Use `--save-temps` with `clang`. Compare both generated `.i` files and write down the differences you observe.

- [ ] Create a website using GitHub Pages with `.md` files only. It must have an `index.html` and a sub-page. Download the build artifacts and compare `README.md` with the generated `index.html` — write down the differences.

