### November 2025

#### 3 Nov 2025

- [ ] `man echo` — how do you suppress the trailing newline in `echo`?
- [ ] Explore `exiftool` using `exiftool <filename>`, then change permissions using `chmod`.
- [ ] Run a program that takes input from `input.txt`, saves output to `output.txt`, and saves errors to `error.txt` using redirection operators.
- [ ] `man tac`, `man tail`, `man head`, `man more`.
- [ ] Write five integers to a file in C, read them back from the file, and print them to stdout.
- [ ] Run `cat <existing-filename> <non-existing-filename>` and record your errors and observations.
- [ ] Write a C program to print the ASCII chart.
- [ ] Write a C program to print the bytes of an integer in Little Endian style and then in Big Endian style.
- [ ] Explore logic gates and 2's complement.

---

#### 4 Nov 2025

- [ ] Write a C program that generates errors and stores them in a file.
- [ ] Create a file and a directory inside a directory. Run `ls -l` and observe the output.
- [ ] Explore `readelf` and observe the metadata of a binary file using options `-a` and `-h`.

---

#### 5 Nov 2025

- [ ] Install all the tools used in the remote cloud onto your local virtual machine using a single command.

---

#### 10 Nov 2025

- [ ] Stop and restart the `sshd` service.

---

#### 11 Nov 2025

- [ ] Explore the open-source code of the Linux kernel and TCP implementation.
- [ ] Explore `netcat` and `curl`.

---

#### 12 Nov 2025

- [ ] Explore developer resources from the Google I/O and WWDC 2025 events.
- [ ] What do the latest SDKs from Google I/O and WWDC contain?
- [ ] What are other SDKs available for developers?
- [ ] Read the *Introduction to HTTP* book by Launch School.
- [ ] List 4 responsive websites and 4 non-responsive websites.

---

#### 17 Nov 2025

- [ ] Run `netcat -l` on a remote server and a VM, then make a request and observe the raw HTTP headers.
- [ ] What is Ed25519? What do the public and private keys contain, and how are they generated?
- [ ] How long would a supercomputer take to break modern encryption?
- [ ] What is a checksum and how is it computed?
- [ ] Run a Python HTTP server on Ubuntu and try to connect from a friend's browser. What happens — does it connect? If not, why?
- [ ] Explore port forwarding to understand how two machines can transfer data.

---

#### 22 Nov 2025

- [ ] Explore the `pkill` command — why and where is it used?
- [ ] What types of SoC chips are available in your mobile phone? Which company created them? Do they take a licence from ARM?
- [ ] Is a JVM running on your mobile phone?

---

#### 24 Nov 2025

- [ ] Create the following `sample.py` file and inspect its disassembly:
  ```python
  num1 = 2
  num2 = 3
  num1 = num1 + num2
  print(num1)
  ```
- [ ] Write Python code that deliberately generates errors.
- [ ] Debug `sum.py` using the Python Debugger (PDB).

---

#### 25 Nov 2025

- [ ] Find a sample open-source project and try to understand its code structure.
- [ ] Where can you find open LLM models and how do you use them?
- [ ] Complete two contributions to any open-source project.

---

#### 26 Nov 2025

- [ ] Explore basic SQLite using SQL commands.
- [ ] Explore REPL-type shells and playgrounds for other languages: Java, C, SQL, JavaScript, etc.
- [ ] Create a table with `name` and `height` columns. Inspect the `.db` file using `xxd` before and after inserting data.
- [ ] In SQLite, experiment with `PRIMARY KEY`.

---

#### 28 Nov 2025

- [ ] Write a SQL query to add a new flight from Visakhapatnam with a duration of 60 minutes into the flight table.
- [ ] Explore `locals()` and `args` in Python PDB.

---
