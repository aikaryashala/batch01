#### 27 Jan 2026

- [ ] Explore keyboard events, mouse events, button events, text box events, radio button events, and checkbox events.
- [ ] Try to encode the information about event types in an optimised structure — e.g., as a list or array.

---

#### 29 Jan 2026

- [ ] HTTPie vs HTTP — what is the difference?
- [ ] Explore Postman.
- [ ] What are all the Linux packages installed on your machine so far?
- [ ] What is an index in a textbook?
- [ ] What is an index in a database?
- [ ] What is an index in an array?
- [ ] Why is the default file name `index.html`?
- [ ] What does the local package index contain in Linux?
- [ ] Install a few random packages on Linux and understand every word in the output after running the install command.
- [ ] What is metadata — explain it in different contexts.
- [ ] Is there metadata in a database?

---

#### 30 Jan 2026

- [ ] When a client connects to the server and the server doesn't send a response within 2 minutes, does the client terminate the connection? Explore how timeout behaviour works.
- [ ] Start a Python service with a debugger and add a breakpoint to a function (`get_students`). Run it, then make a GET request from a client for that specific API. When the breakpoint hits, don't do anything — wait and observe when the client disconnects.
